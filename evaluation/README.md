# 错因诊断评测说明

## 评测集

- 样例数：24 条自建标注样例，不代表真实用户数据。
- 覆盖学段：小学五、六年级；初一至初三；高一至高三。
- 覆盖学科：数学、语文、英语、物理、化学、生物。
- 覆盖标签：知识盲区、题型不熟、计算粗心、状态差、态度问题；状态标签为正常、疲惫、敷衍。

## 首轮基线结果

评测时间：2026-08-16

| 指标 | 结果 |
| --- | --- |
| 结构化输出有效率 | 24/24（100%） |
| 错因分类命中率 | 23/24（95.8%） |
| 状态识别命中率 | 24/24（100%） |
| 平均端到端耗时 | 4.53 秒 |

唯一未命中样例为高一物理的斜面摩擦力方向判断：预期标签为“题型不熟”，模型判为“知识盲区”。该样例中学生明确表示不理解静摩擦力方向，因此模型结论具备合理性；保留该差异，作为标签边界的人工复核项，而非为提升分数强行修改结果。

## 复核方式

运行 `npm run evaluate` 会调用生产诊断接口并更新 `latest-report.json`。每条 Langfuse Trace 会写入 `evaluationCaseId`，可通过该编号与报告中的预期标签、实际输出逐条对应。
